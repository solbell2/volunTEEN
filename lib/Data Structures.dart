class Opportunity {
  String id;
  String name;
  String address;
  String dateTime;
  String description;
  String tags;
  String participants;
  String user;
  String confirmed;
  int hours;

  Opportunity(this.id, this.name, this.address, this.dateTime, this.description, this.tags, this.participants, this.user, this.confirmed, this.hours);
}