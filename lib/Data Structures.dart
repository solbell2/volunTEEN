class Opportunity {
  String id;
  String name;
  String address;
  String dateTime;
  String description;
  String tags;
  String participants;

  Opportunity(this.id, this.name, this.address, this.dateTime, this.description, this.tags, this.participants);
}